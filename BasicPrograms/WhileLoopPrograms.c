#include <stdio.h>
#include <math.h>
int main() {
    // ? Q1:Separate Digits and Calculate their sum
    // int num;
    // printf("Enter a number:");
    // scanf("%d",&num);
    // int sum = 0;
    // while (num > 0){
    //     sum += num%10;
    //     num /= 10;
    // }
    // printf("Sum is: %d\n", sum);

    // ? Q2: Print the reverse of a number
    // int n;
    // printf("Enter a number: ");
    // scanf("%d", &n);
    // int n2 = 0;
    // int temp = n;
    // while (temp > 0) {
    //     n2 = n2 * 10 + (temp % 10);
    //     temp /= 10;
    // }
    // printf("Reverse is: %d\n", n2);

    
    return 0;
}